<!DOCTYPE html>
<html>
  <head>
    <title></title>
  </head>
  <h1>Links with tag: <%= @tag.name %></h1>
  <body>
    <div>
    <ul id='tags'>
      <% @filtered_links.each do |link| %>
      <li>
        Title: <%= link.title %>
        URL: <%= link.url %>
      <% end %>
    </li>
    </ul>
  </div>
  </body>
</html>
